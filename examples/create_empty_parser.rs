//! Creation of a parser which doesn't expect any arguments and will return an error on parsing if any argument is provided.
//! ```rust,editable
//! fn main() {
//!    let parser = arg_parse::ArgParser::new();
//! }
//! ```
fn main() {
    let _parser = arg_parse::ArgParser::new();
}